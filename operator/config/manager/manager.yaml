apiVersion: v1
kind: Namespace
metadata:
  labels:
    control-plane: controller-manager
  name: system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ctrlman
  namespace: system
  labels:
    control-plane: controller-manager
spec:
  selector:
    matchLabels:
      control-plane: controller-manager
  replicas: 1
  template:
    metadata:
      annotations:
        kubectl.kubernetes.io/default-container: manager
      labels:
        control-plane: controller-manager
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
      - command:
        - /manager
        args:
        - --leader-elect
        image: controller:latest
        name: manager
        securityContext:
          allowPrivilegeEscalation: false
        livenessProbe:
          httpGet:
            path: /healthz
            port: 8081
          initialDelaySeconds: 15
          periodSeconds: 20
        readinessProbe:
          httpGet:
            path: /readyz
            port: 8081
          initialDelaySeconds: 5
          periodSeconds: 10
        # TODO(user): Configure the resources accordingly based on the project requirements.
        # More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
        resources:
          limits:
            cpu: 8000m
            memory: 4Gi
          requests:
            cpu: 4000m
            memory: 1Gi
        env:
        - name: RECONCILIATION_TARGET_KUBECONFIG_0
          value: "/etc/manifest-operator-targets-config/kubeconfig--jellyfish--worker1.yaml"
        - name: RECONCILIATION_TARGET_KUBECONFIG_1
          value: "/etc/manifest-operator-targets-config/kubeconfig--jellyfish--worker2.yaml"
        volumeMounts:
        - name: manifest-operator-targets
          mountPath: "/etc/manifest-operator-targets-config"
          readOnly: true 
      serviceAccountName: ctrlman
      terminationGracePeriodSeconds: 10
      volumes:
      - name: manifest-operator-targets
        secret:
          secretName: manifest-operator-targets
          optional: false

